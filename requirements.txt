# Core API -- always required to run the server.
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2

# Providers
openai==1.51.0
anthropic==0.36.0

# Rate limiting / cost tracking use only asyncio + sqlite3, both stdlib --
# no extra dependency needed.

# --- Optional extras, install only what you actually use ---
#
# Semantic cache (faiss + numpy):
#   pip install -r requirements-cache.txt
#
# Trained difficulty classifier (torch + transformers) -- normally only
# needed in Colab, not on the machine running this API, unless you've
# copied a trained checkpoint back and want local inference:
#   pip install -r requirements-ml.txt
#
# Tracing (OpenTelemetry and/or LangSmith):
#   pip install -r requirements-tracing.txt
#
# Benchmark harness (datasets, scikit-learn):
#   pip install -r requirements-benchmark.txt
#
# Load testing:
#   pip install locust
