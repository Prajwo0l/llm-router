# Tracing extra -- app/telemetry/tracing.py imports these lazily; both
# otel_enabled and langsmith_enabled default to False in app/config.py, so
# the server runs fine without this installed.
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp-proto-http==1.27.0
langsmith==0.1.129
